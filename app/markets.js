const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
export function marketCard(x){
  const rows=(x.markets||[]).map(m=>`<div class="market-row" data-market="${esc(m.market)}">${esc(m.text||m.market)}${m.captured_at_utc?`<div class="market-note">Снимок: ${esc(m.captured_at_utc)}${m.api_update_utc?` · источник: ${esc(m.api_update_utc)}`:''}</div>`:''}</div>`).join('');
  return `<article class="match-card" data-league="${esc(x.league)}" data-fixture="${esc(x.fixture_id)}"><div class="eyebrow">${esc(x.league)} · ${esc(x.kickoff_local||x.kickoff_utc)}</div><div class="match-title">${esc(x.home_team)} — ${esc(x.away_team)}</div><div class="match-meta">Матч №${esc(x.fixture_id)} · ${esc(x.fixture_status||'NS')} · нажми для полной карточки</div><div class="market-list">${rows||'<div class="empty">Нет данных о рынках.</div>'}</div><div class="market-note">MARKET VIEW ONLY — не сигнал и не рекомендация. Коэффициенты — собранные снимки, актуальность не гарантирована.</div></article>`;
}
export function leagueOptions(cards){return [...new Set(cards.map(c=>c.dataset.league).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'ru'))}
