name: Player Synergy Forward Gate

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'scripts/player_synergy_forward_gate.py'
      - 'scripts/tests/test_player_synergy_forward_gate.py'
      - 'docs/player_synergy_forward_gate.md'
      - '.github/workflows/player-synergy-forward-gate.yml'

permissions:
  contents: read

concurrency:
  group: player-synergy-forward-gate-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Compile and provider-free tests
        run: |
          python -m py_compile scripts/player_synergy_forward_gate.py
          python -m unittest scripts.tests.test_player_synergy_forward_gate -v
      - name: Provider isolation
        shell: bash
        run: |
          if grep -nE 'urllib\.|requests\.|api-sports\.io|v3\.football\.api-sports' scripts/player_synergy_forward_gate.py; then
            echo 'Synergy forward gate must remain provider-free.' >&2
            exit 1
          fi
      - name: Feature diff hygiene
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          git fetch origin main
          changed="$(git diff --name-only origin/main...HEAD)"
          printf '%s\n' "$changed"
          if printf '%s\n' "$changed" | grep -Eq '^ops/'; then
            echo 'Generated ops outputs are forbidden in the feature PR.' >&2
            exit 1
          fi

  materialize:
    needs: validate
    if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build prospective synergy forward gate
        env:
          OPS_DIR: ops
        run: python scripts/player_synergy_forward_gate.py
      - name: Publish synergy forward evidence safely
        shell: bash
        run: |
          git config user.name 'player-synergy-forward-gate-bot'
          git config user.email 'player-synergy-forward-gate-bot@users.noreply.github.com'
          git add ops/player_synergy_prematch_forward_candidates.csv
          git add ops/player_synergy_promotion_gate.json
          git add ops/player_synergy_final_readiness.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Player synergy forward gate update'
          git restore --worktree -- .
          python scripts/publish_operational_commit.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: player-synergy-forward-gate
          path: |
            ops/player_synergy_prematch_forward_candidates.csv
            ops/player_synergy_promotion_gate.json
            ops/player_synergy_final_readiness.json
          retention-days: 30
